初始密码
用户编码
拨号长度 不应该电话长度 
拨号有显示，单无拨号音

get_serial返回的serial号 n，在之后的请求fileinfo时 传递的不能是n，而是n-1

fileinfo的头结构32字节，实际用到了28个 
fileaddress - 3个字节 
filelen - 2个字节 ，最高位和最低位位0，

phone包含了拨号数字和之后播的分机号等所有按键号码

非法的serial获取文件信息，返回数据都为错误，
确认 擦除flash时默认是否都清0xff



2013.5.22 
-----------------
1. getserial 获取最新的serial，但此刻文件还未生成，这个时候去获取文件信息是okay的，但文件长度错误，导致马上去获取这个文件，获取错误数据 
 解决这个办法在于 获得serial之后去获取文件信息时，如果文件未完成写入，则要提示上位机文件未写完，比如：文件长度或者偏移值设置为0，上位机就不会去获取话机上的语音
2.录音开启和关闭都无hangup消息提示到上位机
